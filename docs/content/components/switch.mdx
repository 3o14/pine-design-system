---
title: Switch
---

# Switch

설정이나 기능을 켜고 끄는 토글 컨트롤입니다.
[Base UI Switch](https://base-ui.com/components/react-switch/)를 기반으로 접근성을 기본으로 지원합니다.

Checkbox와 달리 Switch는 변경이 즉각적으로 반영될 때 사용합니다. 저장 버튼 없이 바로 설정을 적용하는 경우에 적합합니다.

## 미리보기

<ComponentPreview code={`<Switch defaultChecked />
<Switch label="With label" defaultChecked />
<Switch label="Disabled" disabled />`}>
  <SwitchDemo />
</ComponentPreview>

## 예제

### Intent

Switch 트랙의 색상 의도를 결정합니다. 기능이 켜진 상태일 때 표시되는 색상에 영향을 줍니다.

<DocTable
  headers={["값", "기본값", "설명"]}
  rows={[
    ["`primary`", "✓", "일반적인 설정 토글에 사용합니다."],
    ["`secondary`", "", "보조 설정에 사용합니다."],
    ["`success`", "", "활성화가 긍정적 의미를 가질 때 사용합니다."],
    ["`warning`", "", "활성화에 주의가 필요할 때 사용합니다."],
    ["`danger`", "", "활성화가 위험한 동작을 나타낼 때 사용합니다."],
    ["`neutral`", "", "중립적인 토글에 사용합니다."],
  ]}
  align={["left", "center", "left"]}
/>

<ComponentPreview code={`<Switch defaultChecked intent="primary" label="Primary" />
<Switch defaultChecked intent="success" label="Success" />
<Switch defaultChecked intent="warning" label="Warning" />
<Switch defaultChecked intent="danger" label="Danger" />`}>
  <SwitchIntentDemo />
</ComponentPreview>

### Size

Switch의 크기를 결정합니다.

<DocTable
  headers={["값", "기본값", "설명"]}
  rows={[
    ["`small`", "", "밀도 높은 설정 목록에 사용합니다."],
    ["`medium`", "✓", "일반적인 설정 화면에 사용합니다."],
    ["`large`", "", "충분한 클릭 영역이 필요할 때 사용합니다."],
    ["`xlarge`", "", "모바일 환경처럼 큰 터치 영역이 필요할 때 사용합니다."],
  ]}
  align={["left", "center", "left"]}
/>

<ComponentPreview code={`<Switch size="small" defaultChecked label="Small" />
<Switch size="medium" defaultChecked label="Medium" />
<Switch size="large" defaultChecked label="Large" />
<Switch size="xlarge" defaultChecked label="XLarge" />`}>
  <SwitchSizeDemo />
</ComponentPreview>

## 사용 가이드

### Switch vs Checkbox

<DocTable
  headers={["상황", "권장 컴포넌트"]}
  rows={[
    ["켜짐/꺼짐 즉시 반영 (저장 불필요)", <strong>Switch</strong>],
    ["폼 제출 시 최종 반영", <strong>Checkbox</strong>],
    ["여러 항목 중 다수 선택", <strong>Checkbox</strong>],
    ["단일 설정 토글", <strong>Switch</strong>],
  ]}
  align={["left", "left"]}
/>

### 접근성

`label` prop을 통해 스크린리더가 Switch의 목적을 읽을 수 있도록 반드시 레이블을 제공하세요.

## Props

<PropsDoc component="switch" />
