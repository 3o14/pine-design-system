---
title: Dialog
---

# Dialog

중요한 정보를 전달하거나 사용자의 확인·입력을 받는 모달 컴포넌트입니다.
[Base UI Dialog](https://base-ui.com/components/react-dialog/)를 기반으로 포커스 트랩, 스크롤 잠금, Escape 키 닫기를 기본으로 지원합니다.

## 미리보기

<ComponentPreview code={`const [open, setOpen] = useState(false);

<Button intent="primary" onClick={() => setOpen(true)}>Dialog 열기</Button>
<Dialog
  open={open}
  onOpenChange={setOpen}
  title="Dialog 제목"
  description="Dialog 설명 텍스트입니다."
  size="small"
  actions={[
    { label: "취소", variant: "weak", intent: "neutral", onClick: () => setOpen(false) },
    { label: "확인", variant: "solid", intent: "primary", onClick: () => setOpen(false) },
  ]}
>
  <p>Dialog 본문 콘텐츠입니다.</p>
</Dialog>`}>
  <DialogDemo />
</ComponentPreview>

## 예제

### Size

Dialog의 최대 너비를 결정합니다.

<DocTable
  headers={["값", "기본값", "설명"]}
  rows={[
    ["`small`", "", "확인/취소 등 간단한 확인 Dialog"],
    ["`medium`", "✓", "일반적인 폼이나 정보 Dialog"],
    ["`large`", "", "복잡한 폼이나 상세 내용 Dialog"],
    ["`xlarge`", "", "많은 콘텐츠가 필요한 Dialog"],
    ["`full`", "", "전체 화면 Dialog"],
  ]}
  align={["left", "center", "left"]}
/>

<ComponentPreview code={`<Dialog open={open} onOpenChange={setOpen} title="Size: small" size="small" actions={[...]}>
  <p>Dialog 본문입니다.</p>
</Dialog>`}>
  <DialogSizeDemo />
</ComponentPreview>

### Rounded

Dialog 모서리의 곡률을 결정합니다.

<DocTable
  headers={["값", "기본값", "설명"]}
  rows={[
    ["`small`", "", "작은 모서리 반경"],
    ["`medium`", "✓", "기본 모서리 반경"],
    ["`large`", "", "큰 모서리 반경"],
  ]}
  align={["left", "center", "left"]}
/>

## 사용 가이드

### 확인 Dialog

되돌릴 수 없는 액션(삭제, 초기화 등)은 반드시 사용자에게 확인을 받으세요. 이 경우 `actions`에 `danger` intent 버튼을 포함하고 `closeOnOverlayClick: false`로 설정하는 것을 권장합니다.

### 폼 Dialog

폼이 포함된 Dialog에서는 `closeOnOverlayClick: false`를 권장합니다. 사용자가 실수로 입력 내용을 잃지 않도록 보호합니다.

## 코드

```tsx
import { useState } from "react";
import { Dialog, Button } from "pine-design-system";

function DeleteConfirmDialog() {
  const [open, setOpen] = useState(false);

  return (
    <>
      <Button intent="danger" onClick={() => setOpen(true)}>
        삭제하기
      </Button>

      <Dialog
        open={open}
        onOpenChange={setOpen}
        title="정말 삭제하시겠어요?"
        description="삭제된 데이터는 복구할 수 없습니다."
        size="small"
        closeOnOverlayClick={false}
        actions={[
          {
            label: "취소",
            variant: "weak",
            intent: "neutral",
            onClick: () => setOpen(false),
          },
          {
            label: "삭제",
            variant: "solid",
            intent: "danger",
            onClick: handleDelete,
          },
        ]}
      >
        <p>선택한 항목이 영구적으로 삭제됩니다.</p>
      </Dialog>
    </>
  );
}
```

## Props

<PropsDoc component="dialog" />
