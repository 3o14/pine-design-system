---
title: TextField
---

# TextField

텍스트를 입력받는 폼 컴포넌트입니다.
[Base UI Field](https://base-ui.com/components/react-field/)와 [Base UI Input](https://base-ui.com/components/react-input/)을 기반으로 라벨, 입력 영역, 헬퍼 텍스트를 단일 컴포넌트로 제공합니다. `multiline` prop으로 `<textarea>`로 전환할 수 있습니다.

## 미리보기

<ComponentPreview code={`<TextField label="Name" placeholder="Enter your name" />
<TextField label="Email" placeholder="you@example.com" helperText="We'll never share your email." />`}>
  <TextFieldDemo />
</ComponentPreview>

## 예제

### Variant

입력 필드의 시각적 스타일을 결정합니다.

<DocTable
  headers={["값", "기본값", "설명"]}
  rows={[
    ["`outline`", "✓", "테두리가 있는 일반적인 입력 스타일입니다."],
    ["`filled`", "", "배경이 채워진 입력 스타일입니다. 다른 폼 요소와 구분이 필요할 때 사용합니다."],
  ]}
  align={["left", "center", "left"]}
/>

<ComponentPreview code={`<TextField label="Outline" variant="outline" placeholder="Outline style" />
<TextField label="Filled" variant="filled" placeholder="Filled style" />`}>
  <TextFieldVariantDemo />
</ComponentPreview>

### Status

입력 필드의 유효성 상태를 표시합니다. `status`에 따라 테두리 색상과 헬퍼 텍스트 색상이 변경됩니다.

<DocTable
  headers={["값", "기본값", "설명"]}
  rows={[
    ["`default`", "✓", "기본 상태입니다."],
    ["`error`", "", "유효성 검사 실패 상태입니다. 헬퍼 텍스트를 오류 메시지로 활용하세요."],
    ["`success`", "", "유효성 검사 성공 상태입니다. 헬퍼 텍스트를 확인 메시지로 활용하세요."],
  ]}
  align={["left", "center", "left"]}
/>

<ComponentPreview code={`<TextField label="Default" status="default" placeholder="Default" />
<TextField label="Error" status="error" helperText="This field is required." placeholder="Error" />
<TextField label="Success" status="success" helperText="Looks good!" placeholder="Success" defaultValue="john@doe.com" />`}>
  <TextFieldStatusDemo />
</ComponentPreview>

### Size

입력 필드의 크기를 결정합니다.

<DocTable
  headers={["값", "기본값", "설명"]}
  rows={[
    ["`small`", "", "밀도 높은 폼에 사용합니다."],
    ["`medium`", "✓", "일반적인 폼에서 사용합니다."],
    ["`large`", "", "충분한 입력 영역이 필요할 때 사용합니다."],
    ["`xlarge`", "", "큰 입력 영역이 필요할 때 사용합니다."],
  ]}
  align={["left", "center", "left"]}
/>

### Rounded

모서리 곡률을 결정합니다.

<DocTable
  headers={["값", "기본값", "설명"]}
  rows={[
    ["`small`", "", "작은 모서리 반경"],
    ["`medium`", "✓", "기본 모서리 반경"],
    ["`large`", "", "큰 모서리 반경"],
  ]}
  align={["left", "center", "left"]}
/>

### Multiline

`true`로 설정하면 `<input>` 대신 `<textarea>`로 렌더링됩니다. `rows` prop으로 기본 표시 행 수를 지정할 수 있습니다.

<ComponentPreview code={`<TextField label="Bio" multiline rows={4} placeholder="Tell us about yourself…" />`}>
  <TextFieldMultilineDemo />
</ComponentPreview>

## 사용 가이드

### 에러 상태 표시하기

`status="error"`와 `helperText`를 함께 사용해 구체적인 오류 원인을 안내하세요. 단순히 빨간 테두리만 표시하는 것보다 사용자가 무엇을 수정해야 하는지 명확하게 알 수 있습니다.

```tsx
<TextField
  label="이메일"
  status="error"
  helperText="올바른 이메일 형식이 아닙니다."
  value={email}
  onChange={(e) => setEmail(e.target.value)}
/>
```

## 코드

```tsx
import { TextField } from "pine-design-system";

// 기본 사용
<TextField label="이름" placeholder="홍길동" />

// 유효성 상태
<TextField
  label="이메일"
  type="email"
  status="error"
  helperText="올바른 이메일 형식을 입력하세요."
/>

// 여러 줄 입력 (textarea)
<TextField
  label="자기소개"
  multiline
  rows={5}
  placeholder="간단하게 소개해 주세요."
/>

// 전체 너비
<TextField label="주소" fullWidth />
```

## Props

<PropsDoc component="text-field" />
