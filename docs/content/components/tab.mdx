---
title: Tab
---

# Tab

관련 콘텐츠를 여러 패널로 나누어 탐색할 수 있게 해주는 네비게이션 컴포넌트입니다.
[Base UI Tabs](https://base-ui.com/components/react-tabs/)를 기반으로 키보드 탐색과 ARIA 패턴을 기본으로 지원합니다.

## 미리보기

<ComponentPreview code={`const tabs = [
  { value: "one", label: "Overview", content: <p>Overview panel content.</p> },
  { value: "two", label: "Details", content: <p>Details panel content.</p> },
  { value: "three", label: "Settings", content: <p>Settings panel content.</p> },
];

<Tab tabs={tabs} />`}>
  <TabDemo />
</ComponentPreview>

## 예제

### Orientation

탭 목록의 배치 방향을 결정합니다.

<DocTable
  headers={["값", "기본값", "설명"]}
  rows={[
    ["`horizontal`", "✓", "탭을 가로로 나열합니다. 일반적인 페이지 내 탭에 사용합니다."],
    ["`vertical`", "", "탭을 세로로 나열합니다. 사이드바 형태의 네비게이션에 사용합니다."],
  ]}
  align={["left", "center", "left"]}
/>

<ComponentPreview code={`<Tab tabs={tabs} orientation="vertical" />`}>
  <TabVerticalDemo />
</ComponentPreview>

## 사용 가이드

### 탭 수 제한

탭이 너무 많으면 사용자가 콘텐츠를 파악하기 어렵습니다. 일반적으로 2~6개 이내로 유지하는 것을 권장합니다. 탭이 그보다 많다면 Dropdown이나 사이드 네비게이션 방식을 고려하세요.

### Controlled vs Uncontrolled

URL 파라미터와 탭 상태를 동기화하거나, 탭 전환 시 외부 동작(데이터 fetch 등)이 필요할 때는 `value` + `onChange`를 사용하세요. 단순한 UI 탭 전환은 `defaultValue`만으로 충분합니다.

## 코드

```tsx
import { Tab } from "pine-design-system";

const tabs = [
  {
    value: "profile",
    label: "프로필",
    content: <ProfilePanel />,
  },
  {
    value: "settings",
    label: "설정",
    content: <SettingsPanel />,
  },
  {
    value: "billing",
    label: "결제",
    content: <BillingPanel />,
    disabled: true,
  },
];

// 기본 사용
<Tab tabs={tabs} />

// Controlled + 수직 방향
<Tab
  tabs={tabs}
  value={activeTab}
  onChange={setActiveTab}
  orientation="vertical"
/>
```

## Props

<PropsDoc component="tab" />
