---
"pine-design-system": patch
---

Add machine-readable docs for AI coding agents: `llms.txt`/`llms-full.txt` (served on the docs site and now also shipped inside the published package), per-prop JSDoc on every component's Props interface, and a W3C DTCG-formatted design tokens JSON export.
