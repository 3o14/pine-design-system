---
"pine-design-system": patch
---

Fix `Text` `intent="neutral"` using the neutral surface (background-fill) token as its
text color instead of the neutral text token, making it unreadable in dark themes.
