---
"pine-design-system": minor
---

Add Tooltip component with support for placement (`side`, `align`), optional arrow, open state control (`open`, `defaultOpen`, `onOpenChange`), hover/close delay, and disabled state. Includes a shared `TooltipProvider` for cross-tooltip grouping behavior.
