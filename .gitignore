# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Generated from docs/public/ via `pnpm run sync-llms` (runs as part of `build`,
# so it's always regenerated fresh before `npm publish`). Source of truth stays
# in docs/public/llms.txt and docs/public/llms-full.txt — do not commit these.
/llms.txt
/llms-full.txt

# TypeScript incremental build cache
*.tsbuildinfo
*.local
storybook-static

# pnpm
.pnpm-store

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
CLAUDE.md

# Next.js
.next
docs/out

# Vercel
.vercel

# oh-my-claudecode
.omc